# OpenCode Telegram Bot 测试手册

> 测试目标：验证所有命令功能正常，测试不同类型的对话方式

## 测试环境准备

### 1. 启动服务
```bash
# 创建一个测试实例（首次执行）
./manage-bots.sh add test-bot

# 启动该实例的 OpenCode Server + Telegram Bot 服务
./manage-bots.sh start test-bot

# 检查实例状态
./manage-bots.sh status test-bot

# 如需前台调试，可分别使用辅助脚本
./opencode-server.sh --env-file instances/test-bot/server.env status
./control.sh --env-file instances/test-bot/bot.env status
```

### 2. 配置测试环境
确保 `.env` 文件包含：
```bash
TELEGRAM_BOT_TOKEN=你的_bot_token
TELEGRAM_TEST_CHAT_ID=967254220
WORKSPACE_PATH=/Users/voidchen/GitProject
```

---

## 第一部分：命令测试

### 基础命令

#### `/start`
**测试目的**：验证 bot 启动流程
**测试步骤**：
```
发送: /start
```
**期望结果**：
- 显示欢迎信息
- 显示可用命令列表
- 提示用户已授权

---

#### `/help`
**测试目的**：验证帮助信息显示
**测试步骤**：
```
发送: /help
```
**期望结果**：
- 列出所有可用命令
- 包含命令描述
- 格式清晰易读

---

#### `/status`
**测试目的**：验证 OpenCode 服务器状态检查
**测试步骤**：
```
发送: /status
```
**期望结果**：
- 显示 OpenCode 服务器状态（运行/停止）
- 显示版本信息
- 显示连接状态

---

### 会话管理命令

#### `/new` - 创建会话
**测试目的**：验证新会话创建
**测试步骤**：
```
# 测试1: 创建默认会话
发送: /new

# 测试2: 创建带标题的会话
发送: /new My Test Session

# 测试3: 指定工作目录
发送: /new /Users/voidchen/GitProject/test-project Test Project
```
**期望结果**：
- 成功创建新会话
- 返回会话 ID
- 显示工作目录

---

#### `/sessions` - 列出会话
**测试目的**：验证会话列表功能
**测试步骤**：
```
发送: /sessions

# 如果有多个会话，测试分页
发送: /sessions 2
```
**期望结果**：
- 显示所有会话列表
- 包含会话 ID、标题、状态
- 支持分页浏览

---

#### `/sessions <index>` - 切换会话
**测试目的**：验证会话切换功能
**测试步骤**：
```
# 假设会话列表显示:
# 1. abc123 - Session 1
# 2. def456 - Session 2

发送: /sessions 2
```
**期望结果**：
- 成功切换到指定会话
- 显示当前会话信息

---

#### `/sessions remove <index>` - 删除会话
**测试目的**：验证会话删除功能
**测试步骤**：
```
# 创建测试会话
发送: /new Test Session to Delete

# 查看会话列表
发送: /sessions

# 删除会话（假设是第 3 个）
发送: /sessions remove 3
```
**期望结果**：
- 成功删除指定会话
- 确认删除消息

---

#### `/remove` - 删除当前会话
**测试目的**：验证删除当前会话功能
**测试步骤**：
```
发送: /remove
```
**期望结果**：
- 删除当前活动会话
- 提示用户选择新会话或创建新会话

---

#### `/rename` - 重命名会话
**测试目的**：验证会话重命名功能
**测试步骤**：
```
发送: /rename My New Session Name
```
**期望结果**：
- 成功重命名当前会话
- 显示新名称

---

#### `/fork` - 分叉会话
**测试目的**：验证会话分叉功能
**测试步骤**：
```
# 先进行一些对话，然后分叉
发送: /fork

# 或指定消息 ID 分叉
发送: /fork abc123
```
**期望结果**：
- 基于当前会话创建分支
- 显示新会话 ID

---

#### `/share` / `/unshare` - 分享/取消分享
**测试目的**：验证会话分享功能
**测试步骤**：
```
发送: /share

发送: /unshare
```
**期望结果**：
- 成功分享/取消分享会话
- 显示分享链接（如果适用）

---

#### `/diff` - 查看变更
**测试目的**：验证 diff 功能
**测试步骤**：
```
# 查看当前会话变更
发送: /diff

# 查看特定消息的变更
发送: /diff abc123
```
**期望结果**：
- 显示代码变更
- 格式化 diff 输出

---

#### `/summarize` - 总结会话
**测试目的**：验证会话总结功能
**测试步骤**：
```
发送: /summarize
```
**期望结果**：
- 生成会话摘要
- 包含主要操作和决策

---

#### `/children` - 查看子会话
**测试目的**：验证子会话查看功能
**测试步骤**：
```
发送: /children
```
**期望结果**：
- 列出当前会话的子会话
- 显示分叉历史

---

### 模型和 Agent 命令

#### `/model` - 查看模型
**测试目的**：验证模型信息查看
**测试步骤**：
```
发送: /model
```
**期望结果**：
- 显示当前模型覆盖
- 显示可用模型列表

---

#### `/model list`
**测试目的**：验证模型列表功能
**测试步骤**：
```
发送: /model list

# 指定 provider
发送: /model list github-copilot
```
**期望结果**：
- 列出所有可用模型
- 按 provider 分组

---

#### `/model <provider/model>` - 设置模型
**测试目的**：验证模型设置功能
**测试步骤**：
```
发送: /model github-copilot/claude-sonnet-4
```
**期望结果**：
- 成功设置模型覆盖
- 显示确认信息

---

#### `/agents` - 查看 Agent
**测试目的**：验证 agent 信息查看
**测试步骤**：
```
发送: /agents
```
**期望结果**：
- 显示当前 agent 覆盖
- 显示可用 agent 列表

---

#### `/agents list`
**测试目的**：验证 agent 列表功能
**测试步骤**：
```
发送: /agents list
```
**期望结果**：
- 列出所有可用 agents
- 包含 build, plan, oracle 等

---

#### `/agents <name>` - 设置 Agent
**测试目的**：验证 agent 设置功能
**测试步骤**：
```
发送: /agents build

发送: /agents plan

发送: /agents oracle
```
**期望结果**：
- 成功设置 agent 覆盖
- 显示确认信息

---

#### `/plan` / `/build` - 快速切换 Agent
**测试目的**：验证快速 agent 切换
**测试步骤**：
```
发送: /plan

发送: /build
```
**期望结果**：
- 快速切换到 plan/build agent
- 显示当前 agent

---

### 文件操作命令

#### `/ls` - 列出目录
**测试目的**：验证目录列表功能
**测试步骤**：
```
# 列出当前目录
发送: /ls

# 列出指定目录
发送: /ls src

# 列出完整路径
发送: /ls /Users/voidchen/GitProject/my-custom-skills
```
**期望结果**：
- 显示目录内容
- 区分文件和目录
- 支持相对和绝对路径

---

#### `/cat` - 读取文件
**测试目的**：验证文件读取功能
**测试步骤**：
```
# 读取 README
发送: /cat README.md

# 读取代码文件
发送: /cat package.json

# 读取长文件（测试分页）
发送: /cat src/bot/handlers/index.ts
```
**期望结果**：
- 显示文件内容
- 支持代码高亮
- 长文件自动分页

---

#### `/search` - 搜索文本
**测试目的**：验证文本搜索功能
**测试步骤**：
```
发送: /search handleMessage

发送: /search "TODO fix"
```
**期望结果**：
- 显示搜索结果
- 包含文件名和行号
- 显示匹配内容上下文

---

#### `/findfile` - 查找文件
**测试目的**：验证文件查找功能
**测试步骤**：
```
发送: /findfile *.ts

发送: /findfile package.json
```
**期望结果**：
- 显示匹配的文件列表
- 支持通配符

---

#### `/symbol` - 查找符号
**测试目的**：验证符号查找功能
**测试步骤**：
```
发送: /symbol OpenCodeClient

发送: /symbol handleMessage
```
**期望结果**：
- 显示符号定义位置
- 支持跳转到定义

---

#### `/git_status` - Git 状态
**测试目的**：验证 Git 状态查看
**测试步骤**：
```
发送: /git_status
```
**期望结果**：
- 显示 Git 工作区状态
- 显示修改、新增、删除的文件

---

### 任务和 Shell 命令

#### `/task` - 提交异步任务
**测试目的**：验证异步任务提交
**测试步骤**：
```
发送: /task 重构 src/bot/handlers/index.ts 中的 handleMessage 函数

发送: /task 优化 package.json 的依赖
```
**期望结果**：
- 提交任务成功
- 返回任务 ID
- 任务完成后通知

---

#### `/shell` - 执行 shell 命令
**测试目的**：验证 shell 命令执行
**测试步骤**：
```
发送: /shell ls -la

发送: /shell pwd

发送: /shell npm --version
```
**期望结果**：
- 执行命令成功
- 显示命令输出
- 错误处理正常

---

#### `/todos` - 查看待办事项
**测试目的**：验证待办事项查看
**测试步骤**：
```
发送: /todos
```
**期望结果**：
- 显示当前会话的所有待办
- 包含完成状态

---

#### `/history` - 查看历史
**测试目的**：验证历史消息查看
**测试步骤**：
```
# 查看最近10条
发送: /history

# 查看指定数量
发送: /history 20
```
**期望结果**：
- 显示历史消息
- 支持分页

---

#### `/abort` - 中止会话
**测试目的**：验证中止功能
**测试步骤**：
```
# 先启动一个长任务
发送: /task 分析整个代码库

# 然后中止
发送: /abort
```
**期望结果**：
- 成功中止运行中的会话
- 清理资源

---

### 项目和配置命令

#### `/cwd` - 查看当前目录
**测试目的**：验证工作目录查看
**测试步骤**：
```
发送: /cwd
```
**期望结果**：
- 显示当前工作目录
- 显示绝对路径

---

#### `/projects` - 列出项目
**测试目的**：验证项目列表功能
**测试步骤**：
```
发送: /projects
```
**期望结果**：
- 列出所有项目
- 显示项目路径

---

#### `/commands` - 列出 OpenCode 命令
**测试目的**：验证内置命令查看
**测试步骤**：
```
发送: /commands
```
**期望结果**：
- 列出 OpenCode 内置命令
- 包含命令描述

---

#### `/config` - 查看配置
**测试目的**：验证配置查看功能
**测试步骤**：
```
发送: /config
```
**期望结果**：
- 显示当前配置
- 包含模型、agent、路径等

---

#### `/providers` - 列出提供商
**测试目的**：验证提供商列表
**测试步骤**：
```
发送: /providers
```
**期望结果**：
- 列出所有模型提供商
- 显示提供商状态

---

#### `/status_all` - 查看所有会话状态
**测试目的**：验证全局状态查看
**测试步骤**：
```
发送: /status_all
```
**期望结果**：
- 显示所有会话状态
- 包含活跃、空闲、错误状态

---

#### `/tools` - 列出工具
**测试目的**：验证工具列表功能
**测试步骤**：
```
发送: /tools
```
**期望结果**：
- 列出可用工具
- 包含工具描述

---

#### `/init` - 初始化项目
**测试目的**：验证项目初始化
**测试步骤**：
```
发送: /init
```
**期望结果**：
- 分析项目结构
- 创建 AGENTS.md

---

### 配对命令

#### `/pair` - 配对授权
**测试目的**：验证配对功能（需要在未授权账号测试）
**测试步骤**：
```
# 获取配对码（服务器端执行）
./control.sh pair

# 在 Telegram 中发送
发送: /pair <配对码>
```
**期望结果**：
- 成功授权
- 可以正常使用 bot

---

## 第二部分：对话类型测试

### 类型 1：普通对话（自由文本）

**测试目的**：验证基本 AI 对话功能
**测试步骤**：
```
# 先创建会话
发送: /new Test Conversation

# 然后进行对话
发送: 你好，请介绍一下这个项目

发送: 请帮我优化这段代码

发送: 分析一下当前目录的结构
```

**期望结果**：
- AI 正确理解上下文
- 返回相关回答
- 保持会话连贯性

---

### 类型 2：代码编辑对话

**测试目的**：验证代码编辑和生成
**测试步骤**：
```
# 读取文件
发送: /cat src/bot/index.ts

# 请求修改
发送: 请给这个文件添加注释

# 复杂修改
发送: 重构 handleMessage 函数，添加错误处理
```

**期望结果**：
- 正确理解代码上下文
- 生成高质量代码
- 格式正确（代码块、语法高亮）

---

### 类型 3：调试对话

**测试目的**：验证调试和问题排查
**测试步骤**：
```
# 模拟错误场景
发送: 这个服务启动失败了，请帮我排查

发送: 检查一下为什么 OpenCode 连接不上

发送: 分析日志文件中的错误
```

**期望结果**：
- 提供系统诊断
- 检查服务状态
- 给出解决方案

---

### 类型 4：长文本处理

**测试目的**：验证长消息和分页处理
**测试步骤**：
```
# 请求长输出
发送: 请详细解释整个项目的架构设计

发送: 列出所有依赖项及其用途

发送: 生成完整的 README 文档
```

**期望结果**：
- 长消息自动分页
- 格式保持完整
- 不发生消息截断

---

### 类型 5：多轮对话

**测试目的**：验证上下文记忆和连贯性
**测试步骤**：
```
发送: 帮我创建一个用户认证系统

发送: 添加密码加密功能

发送: 现在添加 JWT token 支持

发送: 最后添加角色权限控制
```

**期望结果**：
- 记住之前的上下文
- 累积修改和实现
- 保持代码一致性

---

### 类型 6：工具调用对话

**测试目的**：验证 AI 工具调用（如文件操作、shell 执行）
**测试步骤**：
```
发送: 请帮我创建一个新的配置文件

发送: 运行测试套件并报告结果

发送: 检查代码风格问题
```

**期望结果**：
- 正确调用文件工具
- 正确执行 shell 命令
- 返回执行结果

---

### 类型 7：权限请求对话

**测试目的**：验证权限确认流程
**测试步骤**：
```
# 执行需要权限的操作
发送: 删除 src/test 目录

发送: 修改 package.json

发送: 执行 rm -rf 命令
```

**期望结果**：
- 弹出权限确认按钮
- 等待用户确认
- 执行/拒绝操作

---

### 类型 8：带覆盖的对话

**测试目的**：验证模型/Agent 覆盖
**测试步骤**：
```
# 设置覆盖后对话
发送: /model github-copilot/gpt-4o
发送: 用 GPT-4 的方式解释这段代码

发送: /agents oracle
发送: 深度分析这个架构问题
```

**期望结果**：
- 使用指定模型/agent
- 响应风格相应变化

---

## 第三部分：边界测试

### 边界 1：空输入
```
发送: （空消息，只有空格）
```

### 边界 2：超长消息
```
发送: （复制粘贴很长的一段文本）
```

### 边界 3：特殊字符
```
发送: <script>alert('test')</script>

发送: SELECT * FROM users; DROP TABLE users;

发送: ${PWD} && rm -rf /
```

### 边界 4：并发请求
```
# 快速发送多条消息
发送: 消息1
发送: 消息2
发送: 消息3
```

### 边界 5：网络中断
```
# 测试断网恢复
1. 开始对话
2. 断开网络
3. 发送消息
4. 恢复网络
```

---

## 第四部分：测试报告模板

### 测试记录表

| 测试项 | 命令/输入 | 期望结果 | 实际结果 | 状态 | 备注 |
|--------|-----------|----------|----------|------|------|
| 基础命令-/start | /start | 显示欢迎信息 | | ⏳ 待测 | |
| 基础命令-/help | /help | 显示命令列表 | | ⏳ 待测 | |
| 基础命令-/status | /status | 显示服务器状态 | | ⏳ 待测 | |
| 会话管理-/new | /new Test | 创建新会话 | | ⏳ 待测 | |
| 会话管理-/sessions | /sessions | 列出会话 | | ⏳ 待测 | |
| 文件操作-/ls | /ls | 列出目录 | | ⏳ 待测 | |
| 文件操作-/cat | /cat README.md | 读取文件 | | ⏳ 待测 | |
| 模型命令-/model | /model | 显示模型 | | ⏳ 待测 | |
| Agent命令-/agents | /agents | 显示agents | | ⏳ 待测 | |
| 任务命令-/task | /task 测试 | 提交任务 | | ⏳ 待测 | |
| Shell命令-/shell | /shell pwd | 执行命令 | | ⏳ 待测 | |
| 普通对话 | 你好 | AI回复 | | ⏳ 待测 | |
| 代码编辑 | 请优化代码 | 生成代码 | | ⏳ 待测 | |
| 长文本 | 详细解释... | 分页输出 | | ⏳ 待测 | |

### 状态图例
- ⏳ 待测
- ✅ 通过
- ❌ 失败
- ⚠️ 部分通过
- 🔄 待重测

---

## 快速测试清单

### 必测项（最少测试）
```
✅ /start - 启动
✅ /help - 帮助
✅ /status - 状态
✅ /new - 创建会话
✅ /sessions - 列出会话
✅ /ls - 列出文件
✅ /cat - 读取文件
✅ /model - 查看模型
✅ /agents - 查看agent
✅ /task - 提交任务
✅ /shell - 执行shell
✅ 普通对话 - 自由文本
```

### 完整测试（建议）
所有命令和对话类型都测试一遍。

---

## 问题记录

### 发现的问题

**问题1**: 
- 描述: 
- 重现步骤: 
- 期望: 
- 实际: 
- 严重性: 

**问题2**: 
- 描述: 
- 重现步骤: 
- 期望: 
- 实际: 
- 严重性: 

---

## 总结

测试完成后填写：

- 测试日期: 
- 测试人员: 
- OpenCode 版本: 
- Bot 版本: 
- 通过项数: / 
- 失败项数: / 
- 待修复问题: 
- 备注: 
